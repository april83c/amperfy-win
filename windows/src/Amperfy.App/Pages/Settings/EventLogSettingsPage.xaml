<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Amperfy.App.Pages.Settings.EventLogSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Amperfy.App.Pages.Settings">
    <Grid Margin="24,0,24,16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="BackButton" VerticalAlignment="Center" ToolTipService.ToolTip="Support">
                <FontIcon FontSize="14" Glyph="&#xE72B;" />
            </Button>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Event Log" />
                <TextBlock x:Name="CountText" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="RefreshButton" ToolTipService.ToolTip="Refresh">
                    <FontIcon FontSize="14" Glyph="&#xE72C;" />
                </Button>
                <Button x:Name="CopyAllButton" Content="Copy all" />
                <Button x:Name="ExportButton" Content="Export" />
                <Button x:Name="OpenFolderButton" Content="Open log folder" />
                <Button x:Name="ClearButton" Content="Clear" />
            </StackPanel>
        </Grid>
        <TextBlock
            x:Name="HintText"
            Grid.Row="1"
            Style="{StaticResource CaptionTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="Click an entry to show its details. Right click to copy it." />
        <ListView
            x:Name="EntriesList"
            Grid.Row="2"
            IsItemClickEnabled="True"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:LogEntryItem">
                    <StackPanel Padding="0,8" Spacing="2">
                        <TextBlock MaxLines="3" Text="{x:Bind Message}" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" />
                        <Grid>
                            <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Foreground="{x:Bind TypeBrush}" Text="{x:Bind TypeText}" />
                            <TextBlock HorizontalAlignment="Right" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind DateText}" />
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock
            x:Name="EmptyText"
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="No events logged."
            Visibility="Collapsed" />
    </Grid>
</Page>
