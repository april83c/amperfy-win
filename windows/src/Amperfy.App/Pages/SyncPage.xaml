<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Amperfy.App.Pages.SyncPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <StackPanel Width="440" Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="ms-appx:///Assets/AppIcon.png" Width="96" Height="96" HorizontalAlignment="Center" />
            <TextBlock Text="Syncing library" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center" />
            <TextBlock x:Name="StatusText" Text="Connecting…" Style="{StaticResource BodyStrongTextBlockStyle}" HorizontalAlignment="Center" />
            <ProgressBar x:Name="Progress" IsIndeterminate="True" />
            <TextBlock x:Name="CountText" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
            <TextBlock TextWrapping="Wrap" TextAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="The initial sync downloads the library metadata (artists, albums, playlists, podcasts). Songs of albums are synced in the background later. You can skip this step and continue with a partial library." />
            <Button x:Name="SkipButton" Content="Skip" HorizontalAlignment="Center" Click="SkipButton_Click" />
        </StackPanel>
    </Grid>
</Page>
